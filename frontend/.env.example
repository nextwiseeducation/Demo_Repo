# Left as "/api" in dev — the Vite proxy (vite.config.ts) forwards it to the
# local Django server, sidestepping the backend's missing CORS config.
# In staging/prod, point this at the deployed API's own origin.
VITE_API_BASE_URL=/api
