from django.shortcuts import render

# Untouched Django scaffolding — apps.core has no views. It exists purely to
# hold shared abstractions (see models.py) that other apps import from, not
# to expose any endpoints of its own.
# Create your views here.
