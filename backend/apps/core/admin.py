# Nothing to register — apps.core has no concrete models (see models.py:
# UUIDPKMixin/TimeStampedMixin are abstract and produce no table of their
# own), so there's nothing here for the Django admin to expose.
# Register your models here.
