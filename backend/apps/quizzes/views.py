# Untouched Django scaffolding — no views yet. QuizSession/StudentResponseLog
# (models.py) exist as of Milestone 1 so the schema is ready, but the actual
# quiz-taking endpoints (create session, submit answer, complete session) are
# Milestone 3 ("Quiz Engine") scope, not built yet.
# Create your views here.
