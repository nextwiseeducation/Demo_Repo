from django.shortcuts import render

# Untouched Django scaffolding — no views yet. The Question/AnswerChoice/NGN
# stub models (models.py) exist as of Milestone 1, but the REST endpoints to
# list/filter/serve them (GET /api/questions/, etc.) are Milestone 2/3 scope
# per CLAUDE.md's phase breakdown, not built yet.
# Create your views here.
